<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- For edittext color -->

    <style name="TextAppearence.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="colorControlNormal">@color/appbackround</item>
        <item name="colorControlActivated">@color/appbackround</item>
        <item name="colorControlHighlight">@color/appbackround</item>
        <item name="android:textColorHint">@color/appbackround</item>
        <item name="android:textColor">@color/doveaz_orange</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="AppDialogTheme" parent="Theme.AppCompat.DialogWhenLarge">
        <item name="windowNoTitle">true</item>
        <item name="android:background">@color/appbackround</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        //Your custom background color
    </style>

    <style name="Theme.AppCompat.Translucent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>


</resources>
